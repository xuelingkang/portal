spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: portal
        username: nacos
        password: portal

log:
  home: /data/logs/portal/gateway # 日志目录
