spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

  datasource:
    url: jdbc:mysql://localhost:3306/portal_queue?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf8
    username: root
    password: root

  redis:
    host: localhost
    port: 6379
    password: portal
    database: 5
    timeout: 5000

swagger2:
  enable: true

log:
  home: /data/logs/portal/queue
  root-level: INFO
