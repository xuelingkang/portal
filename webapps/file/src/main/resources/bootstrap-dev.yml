spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

  datasource:
    url: jdbc:mysql://localhost:3306/portal_file?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf8&rewriteBatchedStatements=true
    username: root
    password: root

  redis:
    host: localhost
    port: 6379
    password: portal

sftp-client:
  host: localhost
  port: 122
  username: portalfile
  password: portal

swagger2:
  enable: true

log:
  home: /data/logs/portal/file
  root-level: INFO
