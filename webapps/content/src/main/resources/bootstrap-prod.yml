spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

  datasource:
    url: jdbc:mysql://ali-server02:3306/portal_content?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf8
    username: root
    password: root

  redis:
    host: ali-server01
    port: 6379
    password: portal

  data:
    elasticsearch:
      cluster-name: docker-cluster
      cluster-nodes: ali-server03:9300
      repositories:
        enabled: true

swagger2:
  enable: false

log:
  home: /var/log/portal/content
