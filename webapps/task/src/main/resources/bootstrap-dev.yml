spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

  datasource:
    url: jdbc:mysql://localhost:3306/portal_task?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf8&rewriteBatchedStatements=true
    username: root
    password: root

  redis:
    host: localhost
    port: 6379
    password: portal

swagger2:
  enable: true

log:
  home: /Volumes/Data/logs/portal/task
  root-level: INFO
